[build]
  command = "npm install --legacy-peer-deps --platform=linux --arch=x64 sharp && pnpm build"
  publish = "dist"

[build.environment]
  SHARP_IGNORE_GLOBAL_LIBVIPS = "1"
  PYTHON_VERSION = "3.12.3"
  NODE_VERSION = "25.6.1"
